# TypeArena Environment Configuration
# TypeArena is built as a local-first, client-side application.
# No secret API keys, authentication credentials, or database connections are required for basic deployment.
#
# Optional future extensions:
# VITE_APP_URL=https://your-domain.vercel.app
